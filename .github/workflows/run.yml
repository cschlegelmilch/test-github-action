name: "run action"
on:
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

jobs:
  run-locally:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          milliseconds: 2000

  run-remotely:
    runs-on: ubuntu-latest
    steps:
      - uses: cschlegelmilch/test-github-action@v1
        with:
          milliseconds: 1000
